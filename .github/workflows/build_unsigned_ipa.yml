name: Build iSH-AOK Unsigned IPA for TrollStore
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ipa:
    runs-on: macos-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: 编译并生成.app
        working-directory: ${{ github.workspace }}
        run: |
          xcodebuild build \
            -project iSH-AOK.xcodeproj \
            -scheme iSH-AOK \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ARCHS="arm64" \
            SYMROOT=./build

      - name: 打包为无签名IPA
        working-directory: ${{ github.workspace }}
        run: |
          mkdir -p Payload
          cp -R ./build/Release-iphoneos/iSH-AOK.app Payload/
          zip -r iSH-AOK-Unsigned.ipa Payload/
          rm -rf Payload

      - name: 上传IPA产物
        uses: actions/upload-artifact@v4
        with:
          name: iSH-AOK-Unsigned-IPA
          path: ${{ github.workspace }}/iSH-AOK-Unsigned.ipa  # <--- 这里也使用绝对路径
          retention-days: 7
